name: Build Docker Container and execute them

on:
  push:
    branches: [ "librespot_0.6.0" ]

jobs:
  push_to_docker_hub:
    runs-on: ubuntu-latest
    steps:
    -
      name: Checkout
      uses: actions/checkout@v4
    -
      name: Set up QEMU
      uses: docker/setup-qemu-action@v3
    -
      name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
    -
      name: Login to DockerHub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}
    -
      name: Build Dockerfile.yocto and push librespot_0.6.0 tag

      uses: docker/build-push-action@v6
      with:
        context: .
        file: Dockerfile.yocto
        push: true
        tags: knallwinkel/yocto_librespot_0.6.0:latest
    -
      name: Build Dockerfile.SpotyPee and push librespot_0.6.0 tag

      uses: docker/build-push-action@v6
      with:
        context: .
        file: Dockerfile.SpotyPee
        push: true
        build-args: |
          SPOTYPEE_LABEL=librespot_0.6.0
        tags: knallwinkel/spotypee:librespot_0.6.0
