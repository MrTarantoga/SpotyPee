name: Build Docker Container and execute them

on:
  push:
    branches: [ "walnascar" ]

jobs:
  push_to_docker_hub:
    runs-on: ubuntu-latest
    steps:
    -
      name: Checkout
      uses: actions/checkout@v6
    -
      name: Set up QEMU
      uses: docker/setup-qemu-action@v4
    -
      name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v4
    -
      name: Login to DockerHub
      uses: docker/login-action@v4
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}
    -
      name: Build Dockerfile.yocto and push walnascar tag

      uses: docker/build-push-action@v7
      with:
        context: .
        file: Dockerfile.yocto
        push: true
        tags: knallwinkel/yocto_walnascar:latest
    -
      name: Build Dockerfile.SpotyPee and push walnascar tag

      uses: docker/build-push-action@v7
      with:
        context: .
        file: Dockerfile.SpotyPee
        push: true
        build-args: |
          SPOTYPEE_LABEL=walnascar
        tags: knallwinkel/spotypee:walnascar
